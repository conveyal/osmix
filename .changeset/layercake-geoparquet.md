---
"@osmix/layercake": minor
---

Add `@osmix/layercake` package for importing OSM data from OpenStreetMap US Layercake GeoParquet files.

Features:
- `fromLayerCake()` function to create Osm indexes from GeoParquet files
- WKB geometry parsing for Point, LineString, Polygon, and MultiPolygon
- Support for file paths, URLs, and ArrayBuffer inputs
- Customizable column mapping for different parquet schemas
